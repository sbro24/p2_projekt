export function router(req, res, data) {
    switch (req.url) {
        case '/register':
            
            break;

        case '/register/register':
            
            break;
        default:
            break;
    }
}